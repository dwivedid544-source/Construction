class LocationTracker {
    constructor() {
        this.socket = null;
        this.watchId = null;
        this.user = null;
    }

    init() {
        // Disabled GPS location tracking
    }

    startTracking() {
        // Disabled GPS location tracking
    }

    sendLocation() {
        // Disabled GPS location tracking
    }

    stopTracking() {
        // Disabled GPS location tracking
    }
}

export const locationTracker = new LocationTracker();
export default locationTracker;
